<% flash.each do |key, value| %>
  <% if key == 'notice' %>
    <div class="alert alert-primary"><%= value %></div>
  <% elsif key == 'alert' %>
    <div class="alert alert-danger"><%= value %></div>
  <% end %>
<% end %>
